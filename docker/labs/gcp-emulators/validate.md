# Validation — GCP Emulators

## Quick Check

```bash
# Verify services are running
./run.sh status

# Check logs for errors
./run.sh logs 2>&1 | grep -i "error" | tail -10
```

Follow the validation steps described in tasks.md for each task.
